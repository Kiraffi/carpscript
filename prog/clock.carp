fn fib(nn: i32)
{
    if (nn < 2)
    {
        return nn;
    }
    return fib(nn - 2) + fib(nn - 1);
}

let start = call clock();
print "fib";
print fib(25);
print call clock() - start;

print "Add native";
print call addNative(1.5, 2.25);

let natstr = call stringNative();

print natstr;
print natstr;
